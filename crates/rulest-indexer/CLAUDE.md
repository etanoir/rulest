# rulest-indexer

## Responsibilities

syn-based source code indexer for rulest architecture registry

## Dependencies

This crate depends on: syn, quote, cargo_metadata, walkdir, serde, serde_json, rusqlite, rulest-core

## Ownership

This crate owns all symbols within its `src/` directory.
Do not place symbols here that belong to other crates.
